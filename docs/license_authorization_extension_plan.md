# 授权字段扩展开发计划与进度跟踪

**关联设计文档**：`/home/runner/work/ai_platform/ai_platform/docs/design/license_service.md`  
**计划版本**：v1.0  
**最后更新**：2026-04-07

---

## 1. 目标

围绕授权新增字段完成设计、实现、验证与进度跟踪：

1. 操作系统（`windows/linux/android/ios`）
2. 操作系统最低系统版本（可选）
3. 系统架构（可选）
4. 应用名称（标识用途）

同时保证：

- 历史授权兼容可读
- 新签发授权可完整写入并签名
- Runtime 能按照授权约束校验运行环境
- 文档、计划和阶段总结可持续更新

---

## 2. 模块拆分与实际进度

| 模块 | 目标 | 状态 | 实际进度 |
|------|------|------|----------|
| M1 设计文档 | 更新授权格式、校验策略、模块边界 | 已完成 | 已补充新增字段、环境约束策略、模块改造范围 |
| M2 开发计划 | 建立可跟踪的模块计划、进度与阶段总结 | 已完成 | 已建立本计划文档并回填实际进度 |
| M3 授权后端 | 扩展 Schema、数据库字段、签名载荷、迁移 | 已完成 | 已支持新增字段签发、存储、查询与旧库迁移 |
| M4 授权前端 | 扩展授权生成与列表展示 | 已完成 | 已支持新增字段录入、摘要确认、列表展示 |
| M5 Runtime / Tool | 扩展 license_core、license_tool、Runtime 校验 | 已完成 | 已支持新增字段解析与运行环境约束校验 |
| M6 验证与总结 | 增补测试、执行构建/回归、沉淀阶段总结 | 已完成 | 已补充测试并完成本轮验证与总结 |

---

## 3. 模块执行清单

### M1 设计文档

- [x] 更新 License JSON 结构示例
- [x] 补充新增字段字段说明
- [x] 明确运行环境约束策略
- [x] 明确改造模块边界与计划文档入口

### M2 开发计划

- [x] 建立模块级改造计划
- [x] 记录每个模块目标、状态、实际进度
- [x] 支持阶段小结持续追加

### M3 授权后端

- [x] 扩展 Pydantic Schema
- [x] 扩展 SQLAlchemy 模型
- [x] 扩展 SQLite 自迁移逻辑
- [x] 扩展签名载荷与 License 文件落盘
- [x] 保持旧授权记录兼容读取

### M4 授权前端

- [x] 授权生成页新增字段录入
- [x] 授权摘要新增字段展示
- [x] 授权列表新增字段展示
- [x] 保持原有下载/续期/吊销流程不受影响

### M5 Runtime / Tool

- [x] 扩展 license_core 数据结构与 JSON 序列化
- [x] 扩展 license_tool 对新增字段的解析/校验兼容
- [x] 扩展 Runtime 状态输出
- [x] 扩展 Runtime 对 OS / 最低系统版本 / 架构的校验
- [x] 保持历史授权缺失字段时按“不限制”兼容

### M6 验证与总结

- [x] 新增授权后端测试
- [x] 扩展生产服务授权状态回归测试
- [x] 完成 C++ Runtime 目标编译验证
- [x] 完成授权前端构建验证
- [x] 更新总体进度报告与阶段小结

---

## 4. 验证记录

| 验证项 | 结果 | 说明 |
|--------|------|------|
| `python -m unittest discover -s tests/license -p 'test_*.py'` | 通过 | 覆盖后端新字段签发、校验、迁移 |
| `python -m unittest discover -s tests/prod -p 'test_*.py'` | 通过 | 覆盖生产服务授权状态透传 |
| `cd cpp && cmake -B build -S . -G Ninja && cmake --build build --target ai_runtime desktop_recapture_detect` | 通过 | 验证 Runtime 与能力插件编译 |
| `cd license/frontend && npm install && npm run build` | 通过 | 验证授权前端页面构建 |

> 早期基线检查中曾出现依赖未安装导致的 `fastapi` / `vite` 缺失问题；本轮验证已在安装依赖后完成通过。

---

## 5. 阶段小结

### 阶段 1：设计与计划

- 已将新增授权字段纳入正式设计文档。
- 已建立模块化开发计划，可用于后续继续迭代和实时更新。

### 阶段 2：实现

- 授权后端、前端、license_core、Runtime 已完成一致性改造。
- 新字段已进入签名载荷，避免“界面有字段、授权文件无字段”的设计漂移。

### 阶段 3：验证

- 已补充后端与生产侧回归测试。
- 已完成前端构建和 C++ 编译验证。

### 当前结论

- 本轮授权字段扩展已完成计划内模块改造。
- 后续若需要继续细化，可在此文档上继续追加新的模块、状态和阶段总结。
