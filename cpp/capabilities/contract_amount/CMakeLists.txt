# contract_amount 能力插件

add_capability_plugin(
    NAME         contract_amount
    SOURCES      contract_amount.cpp
    HEADERS      contract_amount.h
    DESCRIPTION  "合同额识别"
    COMPANY      "agilestar.cn"
)
