# pose_estimate 能力插件

add_capability_plugin(
    NAME         pose_estimate
    SOURCES      pose_estimate.cpp
    HEADERS      pose_estimate.h
    DESCRIPTION  "人体关键点检测（姿态估计）"
    COMPANY      "agilestar.cn"
)
