# id_card_classify 能力插件
# Phase 3 (recapture_detect) / Phase 6 (其余能力) 实现

add_capability_plugin(
    NAME         id_card_classify
    SOURCES      id_card_classify.cpp
    HEADERS      id_card_classify.h
    DESCRIPTION  "id_card_classify AI 能力插件"
    COMPANY      "agilestar.cn"
)
