# semantic_segment 能力插件

add_capability_plugin(
    NAME         semantic_segment
    SOURCES      semantic_segment.cpp
    HEADERS      semantic_segment.h
    DESCRIPTION  "图像语义分割"
    COMPANY      "agilestar.cn"
)
