# instance_segment 能力插件

add_capability_plugin(
    NAME         instance_segment
    SOURCES      instance_segment.cpp
    HEADERS      instance_segment.h
    DESCRIPTION  "实例分割"
    COMPANY      "agilestar.cn"
)
