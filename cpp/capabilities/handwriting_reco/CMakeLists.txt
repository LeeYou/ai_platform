# handwriting_reco 能力插件
# Phase 3 (recapture_detect) / Phase 6 (其余能力) 实现

add_capability_plugin(
    NAME         handwriting_reco
    SOURCES      handwriting_reco.cpp
    HEADERS      handwriting_reco.h
    DESCRIPTION  "handwriting_reco AI 能力插件"
    COMPANY      "agilestar.cn"
)
