# face_detect 能力插件
# Phase 3 (recapture_detect) / Phase 6 (其余能力) 实现

add_capability_plugin(
    NAME         face_detect
    SOURCES      face_detect.cpp
    HEADERS      face_detect.h
    DESCRIPTION  "face_detect AI 能力插件"
    COMPANY      "agilestar.cn"
)
